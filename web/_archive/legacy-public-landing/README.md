# Legacy public landing (archived)

The previous full marketing homepage lived in `web/app/page.tsx` before the temporary “Under Construction” swap.

Restore by copying from git history, e.g.:

```bash
git show HEAD~1:web/app/page.tsx
```

Or inspect the commit that introduced this `_archive` folder.
